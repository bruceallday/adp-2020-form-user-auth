import { makeStyles } from '@material-ui/styles'

export const useStyles = makeStyles({
    links:{
        color: 'white',
        textDecoration: 'none',
        margin: 5,
    },

    linkDiv:{
        display: 'flex',
        flexDirection: 'row',
    }
})
